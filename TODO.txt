Session Search (add timestamps to commands in the hist file)

Fix -i search bar not filling

Better github runners (not sure how to test those)

Estimate runtimes and scalability of current commands (probably bad), may want to switch to a binary search tree at some point

Consider an actual database for common commands

Add that past works over ssh fine, and that it supports Rocky Linux

look into bug where cargo commands do not show up in past -i (pytest commands are now showing up as well"

Add a man file to the repo and eventually the man command.

###### 
Add the options for the past -i command to sort found commands by most common etc, give the user more control
######

Look into switching to .elf files or something more UNIX standard.

Design/look at a better or more unique way to store history. Such as a fm-index, or something more compressable and searchable. 
In this you could include a flag for past that would then index all of your commands from your history into this new index.

Fake history generator to test search efficiency etc
